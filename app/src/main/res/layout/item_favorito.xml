<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"
    app:cardCornerRadius="12dp"
    app:cardElevation="4dp">

    <!-- Uso un LinearLayout horizontal para alinear la foto, los textos y el botón -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="12dp"
        android:gravity="center_vertical">

        <!-- Aquí cargaré la foto del Pokémon con Glide -->
        <ImageView
            android:id="@+id/ivPokemonFavorito"
            android:layout_width="70dp"
            android:layout_height="70dp"
            android:src="@android:drawable/ic_menu_gallery"
            android:scaleType="fitCenter"/>

        <!-- Coloco el nombre y el número en vertical uno encima del otro -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical"
            android:layout_marginStart="16dp">

            <TextView
                android:id="@+id/tvNumeroFavorito"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="#000"
                android:textSize="14sp"
                android:textColor="#757575"
                android:textStyle="bold"/>

            <TextView
                android:id="@+id/tvNombreFavorito"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Nombre Pokémon"
                android:textSize="20sp"
                android:textStyle="bold"
                android:textColor="#212121"/>
        </LinearLayout>

        <!-- El botón de borrar. Le he puesto el icono oficial de la papelera -->
        <ImageButton
            android:id="@+id/btnEliminarFavorito"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:src="@android:drawable/ic_menu_delete"
            android:background="?attr/selectableItemBackgroundBorderless"
            app:tint="#CC0000"
            android:contentDescription="Eliminar" />

    </LinearLayout>
</androidx.cardview.widget.CardView>